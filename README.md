# Present
Presentations based on `reveal.js`.


## Setup
Create the symlinks in oder to make the plugins reachable for `reveal.js`.
```shell
cd PROJECT_DIR
./setup.sh
```

### Notes
The `d3js` package has not been updated for several years by now. One day it might be necessary to move to another, e.g. [this one](https://github.com/gcalmettes/reveal.js-d3).